---
title: "Vim使用指南"
description: "Vim使用指南"
pubDate: 2015-11-06T16:00:00.000Z
tags: ["Vim"]
lang: zh
slug: 20151107-vim-guide
---

为了向高端Emacs党和Vim党看齐，了解了一下Vim，作为初学者记录了一些简单的用法。

OS: Ubuntu

安装： sudo apt-get install vim-gtk

配置： sudo vim /etc/vim/vimrc

在最后一行开始输入：

set nu // 在左侧行号

set tabstop=4 //tab 长度设置为 4

set nobackup //覆盖文件时不备份

set cursorline //突出显示当前行

set ruler //在右下角显示光标位置的状态行

set autoindent //自动缩进

## Insert模式

按下 i 键进入Insert模式，在Insert模式中，可以进行文字的编辑。

按下Esc键返回Normal模式。

## Normal模式

### 基本操作

*   x 删除当前光标所在的一个字符。
*   :wq 存盘 + 退出(:w 存盘, :q 退出)。
*   dd 删除当前行，并把删除的行存到剪贴板里。
*   p 粘贴剪贴板到当前行的下一行

### 进阶操作

以后学会了慢慢补充。

2015-11-07